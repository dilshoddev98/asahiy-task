import { Stack } from "react-bootstrap";

const Header = () => (
  <Stack className="header-container justify-content-center align-items-center text-center py-5">
    <h1 className="header-title">Money Manager App</h1>
  </Stack>
);

export default Header;
